<div class="hero-cta py-5" style="background-image: url({{ image.src }});">
  <div class="container">
    <div class="row row-main justify-content-between align-items-center"> 
      <div class="content text-center col-12">
        <h1 class="title-header pt-0 mb-2">{{ heading }}</h1>
        <p class="subheading mb-4">{{ body }}</p>

        <p class="subheading pt-0 mb-0">{{ formHeading }}</p>

        <div class="cta-content">{{ contentHtml|raw }}</div>
      </div>
    </div>
  </div>

</div>
